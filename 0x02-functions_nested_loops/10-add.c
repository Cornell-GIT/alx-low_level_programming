#include "main.h"
/**
 * add - this program adds two integers and returns the result
 * @x: the first integer
 * @y: the second integer
 * Return: sum of the integers x and y
 */
int add(int x, int y)
{
	int z;

	z = x + y;

	return (z);
}
