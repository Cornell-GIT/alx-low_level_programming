#include <stdio.h>
/**
 * main - This program will ouput a line using printf followed by a new line
 * Return: output zero (this indicates the end of the program)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
